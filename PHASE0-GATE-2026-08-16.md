# BD 活动雷达 Phase 0 Gate 检查

- 检查时间：2026-08-16T02:42:41-07:00
- Spec：`/Users/stan/Downloads/Codex执行Spec-活动雷达-v1.md` v1
- Gate 结果：`NOT PASSED / 未通过`
- 原始分支判断：按 Spec，Phase 1 应保持锁定。
- 覆盖授权：Stan 于本次任务中明确要求“覆盖 Phase 0 Gate，直接开工 Phase 1”；以下 Phase 1 实现基于该授权，不把 Gate 结果改写成通过。

## 验收检查

| 条件 | 结果 | 证据 |
|---|---|---|
| 连续 2 期真实推送样张 | Not verified | 当前工作区及已检索的 Documents/Downloads 未找到活动雷达推送样张 |
| Stan 对每条活动的漏报/误报标注 | Not verified | 未找到对应标注、评分校准记录或确认文件 |
| 漏报 = 0 | Unknown | 没有两期样张和外部听闻活动对照表，无法计算 |
| 误报 <= 30% | Unknown | 没有 Stan 对 Tier A/B 活动的逐条判断，无法计算 |

依据 Spec，缺少上述证据时不能把 Gate 判为通过，也不能直接启动 Phase 1。

## 已对齐配置

- 推送渠道：Hermes 已连接的 Weixin Bot
- 活动地域：上海
- 部署：GitHub Pages
- 月度预算：不设固定上限；实现时仍保留每次运行成本日志和异常成本停止条件
- LLM：Stan 指定使用电脑中已有的 Codex API key；本轮不读取或打印密钥
- GitHub：Stan 已授权我自行创建仓库并部署；本轮尚未执行外部写入

## Hermes 只读预检

- Hermes：v0.18.0，gateway service running，Weixin configured，home target present
- 当前模型：`gpt-5.6-sol` / `custom:openai`
- Codex auth：Hermes status 未通过，`access_token` 缺失；这与 Stan 口头确认本机存在 API key 不同，后续实现时需在实际调用入口做一次不泄露密钥的 smoke test
- OpenAI API key：Hermes status 未识别为已配置；不把该状态当作 Phase 0 Gate 证据，也不要求 Stan 在聊天中粘贴密钥
- Weixin gateway：曾出现 `nodename nor servname provided` 和 `Server disconnected`，因此不能宣称消息推送链路已验收

## 实现状态（覆盖授权后）

- Phase 1 本地实现已开始；fixture、去重/diff、回测和推送 dry-run 已通过。
- 真实 Responses API smoke test 返回 HTTP 401，真实全量运行暂未通过；不伪称已完成真实运行。
- Hermes 本地发送适配器已实现，但本轮没有发送微信消息。
