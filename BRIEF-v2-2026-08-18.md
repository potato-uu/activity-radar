# BD 活动雷达 v2 执行简报（交 Codex，sol + xhigh）

> 起草：Claude，2026-08-18。项目目录：`/Users/stan/Documents/Stan BD商务/`（Python 包 `activity_radar`，git 远端 `potato-uu/activity-radar`，GitHub Pages 已开）。
> 你只做本简报范围内的事。设计已定，不要改设计；发现事实与设计冲突时，写进 `PROGRESS-v2.md` 的 `Blocked/Deviations` 段并按"停止条件"处理，不要自作主张绕过。

## 0. 一句话目标

v1 的下游（评分卡、去重、时间轴、推送样张）保留，**重做上游"发现层"**：从"让 LLM 带联网搜索去每个网站找"改成"直连信息源适配器 + 年度大会种子日历 + LLM 只做打分/归一化"，让 Stan（星图比特 BD&Marketing）不再错过上海及长三角周边该去的会。v1 失败根因是发现层：真实运行只入库 8 条、0 条 A 级、6/15 个源空手、Google 开发者大会（2026-08-12 上海）从未被捕获、"回测"只是给 fixture 打分。

## 1. 已知事实（已由 Claude 实测，不必重复验证）

- 工作区有未提交改动（并行/超时/tests 等，`git status` 12 个文件）；`origin/main` 比本地多 1 个机器人提交 `cd2ac20`（data/logs/site）。
- `.venv` 缺 pytest；`PYTHONPATH=src python -m activity_radar.cli backtest --fixture ...` 可跑。
- Actions workflow 每周一 00:00 UTC 跑，v1 单次运行 47–55 分钟。
- 仓库现在是 PUBLIC，Pages 在 https://potato-uu.github.io/activity-radar/ 。**不要改可见性。**
- Hermes 微信链路 Stan 正在另一个会话修；`hermes send --to weixin` 目前不保证成功。
- 参考站 OnePilot（https://onepilot.xin/timeline/ ，原 onepilot.zeabur.app 308 跳转过来）：上海 OPC/AI 时间轴，约 500 条，前端渲染，加载 `/supabase-config.js` + `/assets/index-*.js`；它列出了 `2026-08-12 2026 Google 开发者大会`、`2026-11-07 2026 Google Devfest 谷歌开发者节`、多场 WAIC side event、GEO/出海/跨境沙龙。
- 已核实的未来大会：2026 云栖大会 2026-09-22～24 杭州（阿里官方，报名已开）；金投赏 2026 上海 10 月中旬（具体日待官网）；外滩大会 v1 数据为 2026-09-09～12 上海（以官网为准）；进博会每年 11 月 5–10 日上海。
- Stan 的校准反馈（决定评分语义，见 §3）：
  - 6/25 参加的 GEO 峰会：内容全是 GEO、展商大部分是 GEO 同行 → **同行/服务商扎堆 = 低价值**（供给侧密度高、需求侧密度低）。
  - 8/1 左右的医疗科技展：参展商很少 → **规模小且非精英闭门局 = 低价值**。
  - 小型沙龙/meetup 也要，因为有的"知识密度和人才密度很高"——即在场的人（创始人/操盘手）质量高。
  - "去学东西"先放一放，不作为评分驱动。
  - 业务优先级：ChatGPT Ads 投放 ≈ GEO 最高；汇话客 SDK、AaaS 陪跑暂缓。
  - ICP 仍成立：P0 出海投放操盘手/广告主、P1 独立站/亚马逊/Shopify 创始人、P2 新能源/光伏出海负责人。
  - side event 玩法（在大会期间办自己的沙龙/晚宴）要让工具主动识别。
  - 推送：微信；**每周日 18:00 全量 + 每周三 10:00 增量更新**（Asia/Shanghai）。

## 2. 范围与禁区

**范围**
- 地域：上海 = 主场；周边 = 苏州、杭州、南京、宁波、无锡、合肥、嘉兴、南通（高铁 ≤2.5h）。其他国内城市：只有 Tier A 才入库且只进网页不进推送。海外：不入库（v1 的"海外→C"废止，减少噪音）。
- 时间窗：未来 **120 天**（大会提前 2–3 个月放票；v1 的 60 天太短）。
- 类型：展会 / 峰会 / 沙龙·meetup / 开发者大会 / webinar / side_event，六类全收。webinar 允许进推送（v1 的 `webinar_in_push:false` 改为 true），但类型必须显式标出。

**禁区**
- 不登录、不破解反爬、不绕验证码；每个 host 请求间隔 ≥2 秒；总运行 ≤2 次/周；遵守 robots 中明确 Disallow 的路径。允许对 JS 渲染页做无头渲染（Playwright），但只做"像浏览器一样打开公开页"，不模拟登录、不伪造身份。
- 不自动报名/购票；不发任何消息（`radar push --send` 只在 §7 指定的一次验收里执行）。
- 不改仓库可见性、不删 v1 历史数据（`data/events.jsonl` 只追加/更新 status）、不改 `.github/workflows` 以外的 CI 权限。
- 不打印、不写入任何密钥；`.env` 不提交。
- 不做 Phase 2（参会作战卡、参展商名单匹配）。

## 3. 设计（照做）

### 3.1 发现层：适配器架构
`src/activity_radar/adapters/` 每个源一个适配器，统一接口 `fetch(config, window) -> list[RawCandidate]`。`RawCandidate` 至少含：`source_id, raw_title, raw_date_text, date_start?, date_end?, city?, venue?, url, organizer?, raw_excerpt(≤600字), fetched_at`。适配器只负责"取回 + 尽量结构化"，不打分。类型：
- `html_list`：静态列表页 CSS/XPath 抽取。
- `jsonld`：页面 JSON-LD `Event` 抽取。
- `api`：前端公开 JSON 端点。
- `rendered`：Playwright 无头渲染后再走 html_list 规则。
- `calendar_seed`：年度大会种子（§3.2）。
- `llm_sweep`：v1 的 Responses web_search 机制保留为**一个**兜底源（不再逐站），查询词固定在配置里，成本上限见 §6。
- `wechat_search`：如果本机 `agent-reach` 的 WeChat 文章通道可用（先探测 `agent-reach --help` / 其 skill 文档），做关键词搜索适配器；不可用就在 `source-health.json` 标 `unavailable` 并记录原因，不硬做。

`config/sources.yaml` v2 字段：`id, name, adapter, tier(1/2), cadence(weekly/biweekly), enabled, url(s), selectors/params, region_hint, notes, disabled_at?`。**只增不删**，v1 的源保留（可改 adapter 类型）。

**v2 必做的源（按优先级；每个源先做最小可用，别在单个源上超过 45 分钟，卡住就标 blocked 进下一个）**
1. `onepilot`：先看 `/supabase-config.js` 是否暴露匿名只读 REST（那是它给每个访客的公开配置）；能则 `api` 适配；否则 `rendered` 渲染 `/timeline/` 解析。只取 `date_start ≥ today`。这是 v2 最重要的源。
2. `huodongxing`：活动行搜索/城市页（上海、杭州、苏州、南京），关键词：出海、跨境、AI、广告、营销、GEO、开发者、独立站、亚马逊。
3. `luma`：lu.ma 的 Shanghai 发现页与已知日历（如 AIHH-SH）；找前端 JSON 端点，找不到就渲染。
4. `bagevent`（百格活动）与 `hdb`（互动吧）：B2B 会议为主，同关键词。
5. `10times`：Shanghai / Hangzhou / Suzhou 的 advertising、marketing、e-commerce、AI/IT、solar 类展会列表（结构化，好抓）。
6. `jufair`（聚展 `https://www.jufair.com/v-shanghai/m-<月>/`）与 `bendibao`（上海本地宝 `sh.bendibao.com/xiuxian/zhanhuitime/`）：月度展会排期表。
7. 平台官方：`google-developers`（developers.google.com/events + `gdg.community.dev` 的 GDG Shanghai/Hangzhou/Suzhou 章节活动，后者有公开 API）、`microsoft-reactor-shanghai`、`aws-china-events`（amazonaws.cn/events）、`openai-events`、`volcengine/aliyun/baidu` 开发者活动页、`google-ads/think-with-google` 中国营销活动、`meta-出海营销`、`tiktok-for-business` 活动页。每个先做 `html_list`/`jsonld`，抓不到结构就退化为把该 URL 交给 `llm_sweep` 的定向查询。
8. 行业媒体活动页：白鲸出海 `baijing.cn/activity`、雨果网活动、Morketing、亿邦、AMZ123、品牌星球、36氪活动、霞光社、出海同学会。同上策略。

### 3.2 年度大会种子日历 `config/annual_calendar.yaml`
每条：`id, name, typical_month, city, official_url, why_it_matters, last_known_dates`。初始条目（可补充不可删）：WAIC（7 上海）、外滩大会（9 上海）、云栖大会（9 杭州，2026 已定 09-22～24）、金投赏（10 中旬 上海）、进博会（11 上海）、世界互联网大会乌镇峰会（11 嘉兴乌镇）、Google DevFest 上海（11，2026-11-07）、Google 开发者大会（8 上海）、亚马逊云科技中国峰会（6 上海）、亚马逊全球开店卖家峰会（12）、CCEE 雨果跨境电商选品大会（春/秋）、中国国际广告节、上海国际广告技术设备展、亿邦跨境/未来零售大会、Morketing Summit（年末）、SNEC 光伏（6 上海，P2）、ChinaJoy（7–8 上海，低权重）、Microsoft Ignite/OpenAI DevDay（海外，仅作日历提示不入库）。
`calendar_seed` 适配器：对每条种子，若窗口内尚无其他源确认具体日期，则以 `status: expected`、`date_start = 当月 1 日`、`date_precision: month` 入候选，并由 `llm_sweep` 定向核实官网日期；一旦其他源给出确切日期，合并并把 `status` 改 `active`。推送里 `expected` 条目要标"（日期待官宣）"。

### 3.3 归一化 + 去重（规则层，不调 LLM）
- 日期解析支持中文格式（`8月18日`、`2026.09.22`、`9月22日-24日`、`本周日0621`）；解析失败的候选带 `date_precision: unknown` 进 LLM 归一化，仍失败则丢弃并计数。
- 去重键：`normalize(name)`（去空白/标点/emoji/期数）+ `city` + `date_start ±1 天`。
- **周期系列**：同 `normalize(name)`、同城、多个不同日期（如 ShanghAI 每周 meetup、爱搜 GEO 沙龙月度场）→ 合并成一条 `series` 事件，`occurrences: [dates]`，推送只出现一次并注明"每周/每月，下一场 X 日"。这是 v1 重复 4 条的直接修复。
- diff：新增 / 时间或地点变更（保留 v1 逻辑）/ 取消。

### 3.4 评分 v2（`config/scoring.yaml` 重写；LLM 按此打分，temperature ≤0.2）
双线不变，语义调整：
- `acquisition_score`（获客线，0–10）= 需求侧 ICP 密度：P0 出海投放操盘手/广告主/出海品牌 marketing 负责人 0.55；P1 独立站/亚马逊/Shopify 创始人 0.30；P2 新能源/光伏出海 0.15。GEO 的目标客户（品牌方/企业 marketing 负责人）归入 P0。
- `ecosystem_score`（资源线，0–10）：`platform_presence` 0.55（OpenAI/Google/Microsoft/Meta/TikTok/AWS/字节/阿里/百度 官方在场深度：主办 > 讲者 > 展位 > 冠名）；`channel_ecosystem` 0.35（代理商、出海服务商、媒体、园区、协会 = 合作/联合营销对象）；`ai_developers` 0.10（SDK 暂缓）。
- 新增结构化字段（LLM 输出，规则层使用）：`audience_side: demand|supply|mixed`、`scale_hint: 展商数/人数估计或 unknown`、`format: open|closed_door|invite_only`、`is_series: bool`。
- **修正规则（作用于分数上限，规则层执行）**：
  - `audience_side == supply`（同类服务商/工具商扎堆）→ acquisition 上限 4。（Stan 6/25 GEO 峰会教训）
  - 规模小（展商 <30 或人数 <200）且 `format == open`（不是精英闭门局）→ 双线各 -2。（8/1 医疗展教训）
  - 规模小但 `closed_door/invite_only` 且在场为创始人/操盘手 → 不扣分。（"人才密度高的小局"）
  - 纯课程/培训/学习型 → acquisition 上限 3。
  - 城市：上海 0；周边 -1；其他国内 -2（且只进网页）。
  - 闭门需邀请 -1，备注"值得托关系"。
- Tier：A 任一线 ≥8；B 任一线 ≥6；C 双线 4–5；D 双线 <4 不入库。`action`：attend / send_colleague / watch_content / host_side_event。
- **side event 识别**：凡 Tier A/B 且（多日 或 平台官方 或 预计 ≥1000 人）的大会，标 `side_event_opportunity: true`；同城、日期在其 ±2 天内的其他候选自动挂 `related_to: <大会 id>`（OnePilot 上 WAIC 周边十几个局就是这种）。
- 稳定性：每次打分写入 `score_history`；同一事件相邻两次任一线差 >2 → `needs_review: true`，推送里带 ⚠️。

### 3.5 推送 v2（微信表面：短句、少 markdown、不贴 JSON）
- **周日 18:00 全量**：① 🆕 本周新发现（双线分 + 两句理由）② 📅 未来 4 周 Tier A/B（按日期，`expected` 标"日期待官宣"，系列只出一次）③ ⏰ 报名/购票截止 ≤7 天 ④ 🎪 side event 机会（A/B 大会 + 其周边局清单）⑤ 🩺 源健康度一行（连续 4 周无 hit 的源点名；`unavailable` 的源点名）。
- **周三 10:00 增量**：只发 周日以来的 新增/变更/取消 + 截止 ≤7 天；若无变化，仍发一行"本周三无新增，雷达正常，下次周日 18:00"。
- 实现：`radar push --mode full|delta [--send]`；默认 dry-run 落到 `data/push-latest.txt` 与 `data/push-history/`。`--send` 走现有 Hermes 适配器；失败必须显式报错并写 `logs/push.jsonl`，**不允许静默降级**。
- 调度：
  - GitHub Actions：`0 8 * * 0`（周日 16:00 SH，全量研究 + Pages + push-latest full）与 `0 0 * * 3`（周三 08:00 SH，增量研究 + push-latest delta）。留够 v1 实测的 ~1 小时运行时间。
  - 本机 LaunchAgent `com.stan.activity-radar.push`：周日 18:00 与周三 10:00 执行仓库内脚本 `scripts/push_local.sh`（`git pull --ff-only` → `radar push --mode <auto by weekday> --send`），日志到 `logs/launchagent.log`。写 plist 到 `~/Library/LaunchAgents/`，`launchctl bootstrap gui/$(id -u)` 加载，并在 README 写清 `launchctl bootout` 卸载命令。这是 Stan 明确要求的推送时刻，属本任务授权范围。

### 3.6 时间轴网页
沿用 v1 单文件 `site/index.html` 的结构与视觉，只加：城市筛选（上海/周边/其他）、类型 side_event、`expected` 标记、系列折叠、`related_to` 折叠显示、`needs_review` ⚠️。**不要重做视觉**（后续由 Claude 单独过设计闸门）。

## 4. 执行顺序（每步完成写 `PROGRESS-v2.md`，含时间戳、产物、命令、结果）

0. 先 `git status`/`git diff` 看清未提交改动 → 提交为 `chore: keep v1 local improvements`（只 stage 那 12 个文件，不 `add -A`；`.playwright-mcp/` 和 `大正咖啡集团-*/` 加入 `.gitignore`，不提交）→ `git pull --rebase origin main`（`data/*`、`logs/*` 冲突取并集，`site/index.html` 取任意一方，之后会重生成）。在 venv 装 `pytest`、`playwright`（含 `python -m playwright install chromium`）。
1. 适配器框架 + `onepilot` + `calendar_seed` + 归一化/去重/系列合并 + 单测。跑一次 `radar run --live --sources onepilot,calendar-seed`，看 `data/events.jsonl` 真实变化。
2. 评分 v2 + side event + 稳定性 + 单测。
3. 其余源按 §3.1 优先级逐个接，每个源接完立即 `radar run --live --sources <id>` 验证有 hit 或如实记 empty/blocked。
4. 推送 v2（full/delta）+ Actions cron + LaunchAgent + 脚本。
5. 网页字段。
6. 全量真实运行 → 验收（§5）→ 提交（Conventional Commits，英文）。**不要 `git push`**；push 由 Claude 复核后执行。

## 5. 验收（全部必须给出可复现命令与实际输出，不接受"已完成"）

| # | 项 | 通过标准 |
|---|---|---|
| A1 | 单测 | `PYTHONPATH=src python -m pytest -q` 全绿；`python -m compileall -q src`；`git diff --check` 干净 |
| A2 | 全量真实运行 | `radar run --live` 完成；`data/events.jsonl` 中未来 120 天、上海+周边的 active/expected 事件 ≥40 条；Tier A/B ≥8 条；D 级 0 条；系列事件已合并（同名同城不同日期不再多条） |
| A3 | 召回（公开清单，不得手工写入 events.jsonl 或 fixture 造假；Claude 会用原始适配器输出核对） | 以下必须由适配器/种子+核实自动进入：① 2026-11-07 Google DevFest 上海 ② 2026-09-22～24 云栖大会 杭州 ③ 外滩大会 2026 上海（9 月）④ 进博会 2026 上海（11 月）⑤ 金投赏 2026 上海（`expected` 亦可）。≥4/5 |
| A4 | 回溯 | `radar backtest --as-of 2026-07-01 --live-source onepilot`（新命令：用 onepilot 适配器的原始输出，不用 fixture）必须包含 2026-08-12 Google 开发者大会，且评分 Tier ≥B、ecosystem ≥8 |
| A5 | 去重/diff | 连续两次 `run` 不产生重复；人为改一条日期 → status=changed |
| A6 | 推送 | `radar push --mode full` 与 `--mode delta` 各生成样张；`radar push --mode full --send` **执行一次**并如实记录结果（Hermes 修好则送达；没修好则失败日志） |
| A7 | 调度 | Actions cron 改为周日/周三；LaunchAgent 已加载（`launchctl print gui/$(id -u)/com.stan.activity-radar.push` 有输出） |
| A8 | 成本 | `logs/run.jsonl` 每次运行有 usage；`llm_sweep` 单次运行 ≤ 300k input tokens，超过就截断并记录 |
| A9 | 源健康 | `data/source-health.json` 每个源有 `last_result` ∈ {hit, empty, timeout, error, unavailable, blocked} 与原因 |

## 6. 失败分支与停止条件

- 某源 45 分钟内做不出最小可用 → 标 `blocked` + 原因，进下一个；不阻塞整体。
- OnePilot 既无公开 JSON 也渲染失败 → 停下写 `Blocked`，不要尝试绕过任何保护。
- LLM 端点 401/超时 → 检查 `.env` 是否存在（不打印值）；仍失败则 `llm_sweep` 与打分标 `unavailable`，其余源照跑；A2/A3 无法完成时如实写明。
- Playwright 安装失败 → `rendered` 类源全部标 `blocked`，其余继续。
- 任何操作涉及删除用户文件、改仓库可见性、外发消息（A6 那一次除外）、`git push` → 停止并写 `Blocked`。
- 总时长超过 3 小时 → 停止，写清完成到哪一步、如何续跑。

## 7. 回报格式（写入 `PROGRESS-v2.md` 末尾 `Report` 段）

产出（文件清单）/ 怎么跑（命令）/ 副作用（LaunchAgent、Actions、依赖）/ 验收表 A1–A9 逐条 Passed/Failed/Not tested + 证据 / Blocked & Deviations / 下一步建议。

## 8. 续跑补充（Claude，2026-08-18，第二次派发）

- Step 0 已由 Claude 在外层完成：本地分支现在是 `main`（= `origin/main` + `chore: keep v1 local improvements` + merge 提交 `47962c3`）。你**不要再执行任何 git 写操作**（add/commit/rebase/checkout 都不要），只用 `git status`/`git diff` 看状态；提交由 Claude 在里程碑处代做。§4 第 6 步的"提交"改为：在 `PROGRESS-v2.md` 里列出应提交的文件清单与建议的提交信息。
- 你的沙箱只能写项目目录、`/tmp`、`$TMPDIR`。因此：
  - Playwright 浏览器装到项目内：`PLAYWRIGHT_BROWSERS_PATH="$PWD/.pw-browsers" python -m playwright install chromium`（已加入 .gitignore）；运行时同样设该环境变量，并写进 README。
  - LaunchAgent 的 plist 与安装/卸载脚本写在仓库 `scripts/`（`com.stan.activity-radar.push.plist`、`install_launchagent.sh`、`uninstall_launchagent.sh`），**不要**尝试写 `~/Library/LaunchAgents/` 或执行 `launchctl bootstrap`；A7 的加载由 Claude 在外层执行。
  - LLM 密钥：本地没有 `.env`；v1 的 provider 会只读回退到 `~/.codex/auth.json`。沿用 v1 逻辑，不打印、不复制密钥。若读取被沙箱拒绝，把 `llm_sweep`/打分标 `unavailable`，其余源照跑并如实记录。
- 其余全部按 §1–§7 执行。从 §4 Step 1 开始。

## 9. 修复轮（Claude 复核后，2026-08-18 晚，第三次派发）

Claude 在外层复核了 v2 产物：单测 37 绿、发现层真实运行 174 条原始候选、OnePilot/种子日历都工作。但以下问题必须修，按编号逐条做，每条配最小单测，写进 `PROGRESS-v2.md`。仍然：不做 git 写操作；Chromium 在你沙箱起不来（Claude 已在外层验证 Playwright 能正常启动 Chromium 并打开 onepilot.xin，所以 rendered 适配器请用保存的 HTML fixture 做单测，真实渲染由 Claude 外层跑）。

**F1（阻塞级）`.env` 加载顺序**：`RadarConfig.load()` 在读取任何环境变量之前必须先 `load_local_env(root)`（幂等）。现状是 `load_local_env` 只在 `OpenAIResponsesClient.__init__` 里调用，`base_url` 早已被定成 `https://api.openai.com/v1`，导致 `.env` 里的 `CODEX_BASE_URL` 被忽略 → 打分 401（这就是你 Step 1 遇到的 401 的真正原因，本机 `.env` 现已存在且只含 `CODEX_BASE_URL`/`RADAR_MODEL`/`RADAR_PUSH_TARGET`，不含密钥）。加测试：临时 root 里写 `.env` 含 `CODEX_BASE_URL=https://example.test/v1`，`RadarConfig.load(root).base_url` 必须等于它。

**F2（阻塞级）打分分批 + 可续跑**：现状把 174 条候选塞进一次 Responses 调用，180 秒超时后全部丢弃（Claude 实测：`Responses API failed after 2 attempts: The read operation timed out`，`unscored_candidate_count=174`）。改为：每批 ≤15 条、批间有界并发 ≤3、每批独立重试与超时；任一批失败只影响该批；每次 run 把原始候选落盘到 `data/candidates-latest.jsonl`（含 fetched_at 与 source），未评分的落到 `data/candidates-unscored.jsonl`；新增 `radar score --pending` 只对未评分候选补打分并合并入库；`logs/run.jsonl` 每批一条 usage。

**F3 系列误标**：`rules.py:101` 直接采信 LLM 的 `is_series`，导致 10 条年度大会都被标成系列、推送里出现"（系列：2026-09-01）"这种单日期系列。改为：`is_series` 只由规则层合并产生（同 normalize(name)+同城 的不同日期 ≥2 个），LLM 的 `is_series` 只作提示不落库。

**F4 种子与真实条目去重**：`2026 外滩大会`（calendar-seed，expected，09-01）与 `Inclusion·外滩大会`（annual-ai-conferences，09-09）并存未合并。规则：`expected` 种子条目与任何同城、同月（或 ±45 天）、名称 token 重叠（去掉年份/前缀/标点后，任一方的核心词被另一方包含）的确切条目合并，保留确切日期与来源，`status` 改 `active`，`metadata.seed_id` 记录来源种子。对 `2026 云栖大会` 同理（若其他源给出）。加测试覆盖外滩大会这个真实案例。

**F5 推送文案**：`date_precision: month` 的条目显示为 `2026年9月（日期待官宣）`，不要显示假日期 `2026-09-01`；"未来 4 周" 段中 `expected` 条目按其月份判断是否入段；side event 段只对 `tier == A`，或 `tier == B 且多日` 的大会开启（现状把一天的 B 级"虹桥跨境供应链论坛"也标成了 side event 机会）；源健康度"连续 4 周无 hit"必须真的基于 `no_hit_runs >= 4`（现在只跑过 1–3 次就点名了），首次扫描为空的源单独一行"新源尚无命中：…"。

**F6 LaunchAgent 按上海时间触发**：这台 Mac 系统时区是 `America/Los_Angeles`（Claude 实测 `readlink /etc/localtime`），你写的 plist `Weekday=0 Hour=18` 会按太平洋时间触发。改为：plist 用 `StartCalendarInterval` **每小时** `Minute=5` 触发；`scripts/push_local.sh` 调 `radar push --auto --send`；`--auto` 用 `Asia/Shanghai` 计算：周日 18 点档 → `full`，周三 10 点档 → `delta`，其余时刻打印 `skip` 退出 0；同一上海日期+mode 若 `data/push-history/` 已有成功记录则 `skip`（幂等）。单测覆盖三种时刻（用注入的 now）。

**F7 Hermes 路径**：`push.py:87` 用裸 `hermes`；LaunchAgent 环境没有 shell alias（本机 `hermes` 是 zsh alias 指向 `~/.hermes/hermes-agent/venv/bin/hermes`）。改为 `shutil.which("hermes")`，找不到则回退到 `~/.hermes/hermes-agent/venv/bin/hermes`，再找不到就显式报错写 `logs/push.jsonl`。

**F8 Actions 补 Playwright**：`.github/workflows/radar.yml` 在 `pip install .` 后加 `python -m playwright install --with-deps chromium`，并设 `timeout-minutes: 90`；rendered 源在 CI 里才能跑。

**F9 rendered 源切换**：把 `huodongxing`、`luma-shanghai-ai`、`mosu-space`、`10times`、`meetup-shanghai-ai` 改为 `rendered`（先检查各自 robots；10times 若 robots 禁止则保持 blocked 并注明）；每个用保存的 HTML fixture 写解析单测；真实渲染验证留给 Claude。

**F10 环境**：README 明确写：本机运行需 `.env` 中的 `CODEX_BASE_URL`（不含密钥，密钥只读回退 `~/.codex/auth.json`），并给出 `radar score --pending` 与 `radar push --auto` 用法。

完成后在 `PROGRESS-v2.md` 追加 `## 修复轮 Report`：逐条 F1–F10 Passed/Failed/Not tested + 证据 + 应提交文件清单。

## 10. 数据质量轮（Claude 真实运行复核后，2026-08-19，第四次派发）

Claude 在外层完成了真实全量运行（发现 174 条原始候选，分批打分 9 批成功 3 批超时，`score --pending` 再补两轮后 35 条入库、15 条 pending）。真实数据暴露的问题如下，按编号修，每条配最小单测，不做 git 写操作，不起 Chromium，不外发。`data/candidates-latest.jsonl`（174 条真实原始候选）与 `data/events.jsonl`（35 条）就在工作区，**用它们做回归 fixture**（复制需要的条目到 `fixtures/`，不要整文件搬）。

**G1 垃圾候选过滤（阻塞级）**：`events.jsonl` 里出现 3 条 name 为 `Skip to main content` / `跳至主要内容`（source `google-developers-cn`、`aws-china-events`）、`date_start` 为空却评成 A/B；还有 name 为 `报名中`（`cifnews-ccee`）。规则层在打分前必须丢弃：① 无 `date_start` 且无法解析日期的；② 标题 ≤3 个汉字/≤8 个拉丁字符或命中导航词黑名单（`skip to main content|跳至主要内容|报名中|全部|更多|查看更多|登录|注册|menu|home` 等，配置在 `config/scoring.yaml: title_blacklist`）；③ 同时修复这两个适配器的 selector，让它们抓活动卡片而不是页面骨架（用保存的 HTML fixture 测）。推送与网页都不得出现无日期条目。

**G2 城市判定与范围预过滤**：`eMAG2026中国卖家峰会—深圳` 被标 `city=上海`（cifnews 默认城市）。规则：从标题/地点文本里提取城市关键词（深圳/广州/厦门/北京/成都/杭州/苏州/南京/宁波/无锡/合肥/嘉兴/南通/上海……）优先于适配器默认城市；打分**之前**就按范围预过滤：上海+周边保留；其他国内城市只有 `calendar-seed` 或平台官方源的候选才进打分，其余直接丢弃并计数（省 token，也是 §2 的规定）；海外丢弃。

**G3 小局与培训的评分修正**：① 培训正则扩为 `课程|培训|训练营|实训|公开课|体验课|workshop|bootcamp|training`；② `event_type ∈ {沙龙, 沙龙·meetup}` 且 `scale_hint` 未知或 <200 且 `format == open` → acquisition、ecosystem 上限各 7（即最高 B，"视档期"），`closed_door/invite_only` 不设上限；③ 把 `8.22上海Ai 新商业营销变革精准获客实训`（现为 A）、`徐汇滨江AI 出海Drink Chat`（现为 A）作为回归 fixture，修后应为 ≤B。

**G4 种子/真实跨源去重补强**：`2026 Google DevFest 上海`（annual-ai-conferences/seed）与 `⚡️ 2026 Google Devfest 谷歌开发者节`（onepilot，同日 2026-11-07）未合并。名称 token 化时拉丁 token 统一小写、去 emoji/符号，同城 + 日期 ±1 天 + 任一显著 token（长度 ≥4 的拉丁词如 `devfest`，或 ≥2 字中文核心词）相同即合并，保留信息更完整的一方并记录 `merged_from`。

**G5 文章型页面误抓**：`morketing`、`brandstar` 适配器抓的是文章标题（如"对谈13位头部企业高管…Morketing年中盘点"、"高端一定高价？源氏木语…"），不是活动。改为只抓活动栏目 URL（Morketing 活动页/峰会页、品牌星球活动页），抓不到结构就把该源标 `empty` 并写原因，不要把文章当候选。`eventbrite-shanghai-ai` 里 `Shanghai Bike Tour…`、`TEC x PURE健康快闪站` 这类要靠 G2/打分自然淘汰，不必特判。

**G6 失败批次对半拆分**：同一批两次超时/504 后自动对半拆分重试，直到单条；单条仍失败则写 `data/candidates-unscored.jsonl` 并标 `unscorable_reason`，不阻塞其他条目。用 fake client 测：15 条中 1 条"毒丸"，其余 14 条必须被评分。

**G7 推送/网页收口**：推送 full 段落中每条带一行 `类型·城市·格式`（如 `沙龙·上海·闭门`）；A/B 段不出现 G1 过滤对象；`expected` 仍按 F5 文案。

完成后 `PROGRESS-v2.md` 追加 `## 数据质量轮 Report`（G1–G7 逐条 + 全套单测结果 + 应提交文件）。

## 11. 收尾小修（Claude 最终真实运行复核后，2026-08-19，第五次派发）

真实运行现在 28 条入库、零垃圾、零无日期。剩 4 个小问题，按编号修，配最小单测，不做 git 写操作、不起 Chromium、不外发：

**H1 短标题误杀（阻塞级）**：`2026 金投赏`（calendar-seed，expected）被 G1 的"≤3 个汉字"规则过滤掉了。短标题判定改为：去空白/标点后总字符数（汉字+字母+数字一起算）<4 才淘汰；`2026 金投赏`、`WAIC`、`金投赏` 都必须通过。回归 fixture 用 `data/candidates-latest.jsonl` 里的金投赏条目。

**H2 标题里显式年份优先**：`gdg-east-china` 的 `2025年11月15日 Free registration Google DevFe…` 被解析成 `2026-11-15`。日期解析：标题/日期文本里有显式年份时必须用它；解析出的日期早于今天的候选直接丢弃并计入 prefilter `past`。

**H3 合并时保留更强的结构化字段**：DevFest 种子（`开发者大会`、ecosystem 8）与 OnePilot 条目（`沙龙·meetup`、ecosystem 7）合并后保留了后者的类型与分数。合并规则：`event_type` 取更具体/更高层级（开发者大会/峰会/展会 优先于 沙龙·meetup），分数取各线较高值并在 `score_history` 记录两方，`name` 取更完整的一方。

**H4 AMZ123 标题清洗**：`2026拉美跨境电商赋能大会·杭州站 2026-08-27 浙江省杭州市` —— 标题尾部的日期与省市要剥离到 `date_start`/`city`，`name` 只留活动名。

完成后 `PROGRESS-v2.md` 追加 `## 收尾小修 Report`。

## 12. 推送格式轮（Claude 看到真实样张后，2026-08-19，第六次派发）

真实 full 样张 13,331 字、delta 12,644 字，微信单条放不下，而且内容重复。这是推送层的设计修正，按编号做，配单测，不做 git 写操作、不外发、不起 Chromium：

**P1 去重与压缩**：full 模式只有第 1 段"本周新发现"带两句理由（且只列 Tier A/B；C 级合并成一行"另有 C 级 N 条，见网页"）；第 2 段"未来 4 周 A/B"每条只一行 `日期｜名称｜Tier｜获客 x / 资源 y｜类型·城市·格式`，不重复理由；第 1 段已出现的条目在第 2 段仍要列（它是日程视图）但只一行。第 1 段上限 12 条，超出部分合并成"另有 N 条新发现见网页"。
**P2 side event 段收紧**：只对 `tier == A` 且（多日 或 平台官方 或 scale ≥1000）的大会开启 `side_event_opportunity`；`related_to` 只挂到这类大会上（现状把 B 级"TRAE 创造力大赛"当大会挂了 7 个周边局）。段内每个大会最多列 5 个周边局，多的写"等 N 个"。
**P3 源健康度按周计算**：`连续 4 周无 hit` 改为基于日期：`first_scanned` 距今 ≥28 天且 `last_hit` 为空或距今 ≥28 天 才点名；不足 28 天的新源合并成一行"新源观察中：N 个"。`unavailable/异常` 只列 `error/blocked/unavailable`，且同一源不要重复出现两次（现状 `openai-news-events` 出现两次）。
**P4 微信分段发送**：`send_via_hermes` 把消息按段落/条目边界切成 ≤1800 字的块，依次发送；块间等待 ≥35 秒（iLink 有 30 秒冷却）；任一块失败则停止并把失败块号写 `logs/push.jsonl`，不静默；首块前缀 `（1/N）` 等。目标：压缩后 full ≤ 4000 字（2–3 块），delta ≤ 2000 字。
**P5 首周特例**：首次运行所有条目都是"新发现"——`first_seen` 早于本次推送窗口（上次 full 推送时间）的条目不算新；没有上次推送记录时，新发现只取 `first_seen` 在最近 7 天内的。
**P6 网页链接**：推送末尾加一行 `完整时间轴：https://potato-uu.github.io/activity-radar/`。

完成后 `PROGRESS-v2.md` 追加 `## 推送格式轮 Report`，并附压缩后的 full/delta 样张字数。

## 13. 微信排版轮（Stan 看到真实推送后的反馈，2026-08-20，第七次派发）

Stan 反馈：排版丑、缺链接。微信是纯文本表面（无 markdown），排版靠文本设计；URL 独立成行在微信里是可点击蓝链。所有 32 条事件都有真实 `url`（活动行/OnePilot 报名页）。重写 `push.py` 的文案生成，目标格式如下（严格照做，含空行节奏）：

**full 模式**：
```
📡 BD 活动雷达｜M/D 全量

⭐ 必看（A 级）

① M/D 周X｜活动名
获客9 资源6｜展会·上海·公开
一句话理由（截断到 40 字内，句号结尾）
🔗 https://…

② …（同上，每条之间空一行）

📅 值得排期（B 级·未来 4 周）

· M/D 周X｜活动名｜获客7
🔗 https://…

（每条 2 行，条间不空行）

⏰ 报名截止 ≤7 天 / 🎪 side event 机会
（这两段有内容才出现，格式同 B 级两行式）

🌐 完整时间轴 https://potato-uu.github.io/activity-radar/
🩺 源健康：一行
```
**delta 模式**：同风格，段名换"🆕 新增 / ✏️ 变更 / ❌ 取消"，每条 2 行含链接，无变化时保持现有一句话文案 + 时间轴链接。

规则：
- 日期显示 `M/D 周X`（中文星期，Asia/Shanghai）；`expected` 显示 `M月（日期待官宣）`。
- A 级理由压到一行 ≤40 字（从现有 reason 取第一句截断，不调 LLM）。
- 序号用 ①②③…（超过 10 条用 ⑪⑫…，`chr(0x2460+i)` 到 20，再往后退化成 `11.`）。
- 每条链接独立成行、前缀 `🔗 `；没有 http 开头 url 的条目不出链接行。
- `needs_review` 条目在名字后加 ⚠️；系列条目日期后注 `（每周，下一场 M/D）`。
- 分块逻辑（P4）不变：先按段落边界切 ≤1800 字。
- 单测：格式快照测试（固定假数据 → 断言关键行存在与顺序：段头、①、🔗、🌐）；A 级理由截断测试；expected/系列/needs_review 标记测试。旧格式断言按新格式更新，全套保持绿。

禁区同 §2/§8：不做 git 写操作、不外发、不起 Chromium、不抓网页。完成后重生成 dry-run full/delta 样张，`PROGRESS-v2.md` 追加 `## 微信排版轮 Report`（含两份样张字数与文件路径）。

## 14. 全流程 Review + 修复轮（Stan 指令，2026-08-23，第八次派发）

Stan 要求：对现存问题修复 + 整个流程 end-to-end review。你有双重任务：**先修下面已确认的问题（P0），再以审阅者身份把整条链路过一遍（P1）**，包括 Claude 最近内联改的部分（§13 后的 push.py 截断/重试、auto_mode 放宽、tests 日期修复）——提出者≠审阅者，发现问题直说。

### 背景更新（2026-08-23 实测事实）

- 定时推送 LaunchAgent 曾连续 102 次 exit 127：launchd 进程受 macOS TCC 限制读不了 `~/Documents`。已改为运行副本方案：`~/.local/share/activity-radar`（GitHub 克隆 + 自己的 .venv），`~/.local/bin/activity-radar-push.zsh` → 该克隆的 `scripts/push_local.sh`；日志在 `~/.local/share/activity-radar-launchagent.log`。已实测 kickstart 通（pull → skip → exit 0）。
- `auto_mode` 已放宽为周日 ≥18 时 / 周三 ≥10 时（当天补发），幂等靠 `data/push-history/<日期>-<mode>.success` 标记；今天的 full 已人工补发并在运行克隆里放了 `2026-08-23-full.success`。
- 周日 18:00 的正式推送未发出（上述 TCC 故障），Stan 已收到人工补发（下周 1A+6B）。

### P0 已确认必修（每条配单测；在本 dev 仓库改代码，运行克隆的行为靠代码保证，不要手工去改克隆）

**R1（阻塞级）运行克隆的脏拉取死循环**：`logs/run.jsonl`、`logs/push.jsonl`、`data/push-history/*.txt` 都是 git 跟踪文件；克隆里 `radar push --auto --send` 一旦写日志，克隆变脏 → 下次 `git pull --ff-only` 报 "local changes would be overwritten" 失败 → `push_local.sh` 按现逻辑"继续用本地数据"→ **永远用旧数据推送且无人知晓**。修法（设计已定）：`push_local.sh` 在 pull 前执行 `git checkout -- logs data site 2>/dev/null || true`（丢弃对跟踪产物的本地改动；`.success` 幂等标记是未跟踪文件不受影响），pull 仍失败则把失败写进 `logs/push.jsonl`（kind=pull_failed）再继续，推送文案末尾追加一行"⚠️ 数据未更新（git pull 失败）"。同时把 `logs/*.jsonl` 移出 git 跟踪面向未来：`.gitignore` 加 `logs/*.jsonl`，`git rm --cached` 由 Claude 外层执行（你在报告里给出准确命令清单）。注意 Actions workflow 现在 `git add data logs site`——同步改成不再 add logs 的 jsonl。
**R2 历史脏数据一次性清洗**：`data/events.jsonl` 里仍有早期入库的脏名（如 `2026拉美跨境电商赋能大会·杭州站 2026-08-27 浙江省杭州市`）——H4 清洗只作用于新候选。写一个幂等的 `radar migrate --clean-names` 子命令：对现有事件重跑标题清洗/城市推断（不动分数与状态），迁移记录写 `logs/migrate.jsonl`。跑一次并展示前后对比。
**R3 系列/小局评分回归核查**：最新数据里 `一人公司 Coffeechat 160期` 是 B（获客 6）且 `series=0`——按设计它应被系列合并（Coffeechat 157-160 期同名同城多日期）且小型开放沙龙双线封顶 7 后按理由不该到 6+？核查：为什么系列合并没生效（可能：CI 每次全量运行时旧 occurrences 丢失/名字含期数导致 normalize 不同）；`normalize` 应剥离期数（`第?\d+期`、`Vol.?\d+`、`\d+期`）——已有此设计，验证并修复失效点，配真实数据回归测试。
**R4 dev 仓库的 `reimbursements/` 目录**：Stan 的私人文件，加入 `.gitignore`，绝不提交。

### P1 全流程 review（发现即修小问题；大问题写清但先不动）

按消费者视角把链走一遍，每个环节给 Passed / 问题清单：
1. **调度**：Actions cron（周日/周三 08:00 SH 研究）与本机 LaunchAgent（每小时 :05 判定）之间的时序假设是否成立？周三 Actions 08:00 开跑约 1 小时，克隆 10:05 pull 时 Actions 可能还没 commit 完 → delta 用的是上周日数据（audit：这是设计缺陷吗？给结论与建议，先不改 cron）。
2. **push --auto 的幂等与补发**：跨午夜、周日 23:59 启动运行到周一、成功标记写入时机（发送成功前 or 后？失败会不会漏标/误标）。
3. **发送链路**：3 块连发 + 45s 间隔 + 120/300s 重试在 iLink 小时级频控下的存活概率；`（i/N）`前缀块与重试的交互；失败中断后已发块与未发块的状态是否可从日志判断。
4. **数据层**：events.jsonl 只增不删是否被所有路径遵守；`score_history` 是否无限膨胀；`candidates-latest/unscored` 的生命周期。
5. **评分**：抽 10 条现有事件人工核对修正规则是否都按 `config/scoring.yaml` 落地（供给侧封顶、小局封顶、培训封顶、周边 -1）。
6. **Claude 内联改动 review**：`_reason_text` 截断边界、`send_via_hermes` 重试逻辑（120/300 是否会和外层 LaunchAgent 每小时触发叠加造成重复发送风险）、`auto_mode >=` 放宽、tests 日期稳定性修复——逐个给 Passed/问题。
7. **运行克隆方案本身**：两个仓库副本长期漂移的风险与最小对策建议。

### 约束与交付

禁区同 §2/§8：不做 git 写操作（报告里给出应提交文件清单+提交信息）、不外发消息、不起 Chromium、不抓网页、不打印密钥、不改 `~/.local/share/activity-radar` 运行克隆（它只接受 git pull 分发）。全套单测保持绿。完成后 `PROGRESS-v2.md` 追加 `## 全流程审阅轮 Report`：R1–R4 逐条证据 + P1 七项逐项 Passed/问题/建议，按严重度排序。

## 15. 遗留风险加固轮（Stan 批准动工，2026-08-24，第九次派发）

§14 审阅留下的 4 个设计风险，Stan 已批准全部处理。设计如下，逐条实现并配单测；禁区同 §2/§8（不做 git 写操作、不外发、不起 Chromium、不抓网页、不改运行克隆目录）。

**S1 数据新鲜度握手**：
- Actions workflow 在 `radar run --live` 成功后写 `data/research-meta.json`：`{completed_at: <UTC ISO>, git_sha: $GITHUB_SHA, event_count: <int>, mode: <full|delta>}`，随 data 一起 commit。本地 `radar run --live` 也写同文件（git_sha 用 `git rev-parse HEAD` 可得则填，不可得填 null，不做 git 写操作——读命令允许）。
- `push --auto` 增加新鲜度门：读 `research-meta.json` 的 `completed_at`，换算上海日期。若 == 今天 → 正常发送。若不是今天：窗口起点 +4 小时内（周日 <22 时、周三 <14 时）→ 本次跳过，日志 `kind=auto_skip, reason=waiting_fresh_data`（LaunchAgent 每小时会再试）；超过截止 → 照发，但消息末尾加一行 `⚠️ 数据为 M/D 研究结果（今日研究未完成）`。`research-meta.json` 缺失视为"不是今天"。
- 测试：新鲜/等待/超截止兜底/文件缺失四条路径（注入 now 与 meta）。

**S2 分块发送断点续传（durable outbox）**：
- `send_via_hermes` 发送前把整组块写 `data/push-history/<上海日期>-<mode>.outbox.json`：`{chunks: [...], sent: {index: message_id}, created_at}`（outbox 文件加入 .gitignore）。每块成功即更新 `sent`。全部成功 → 写 `.success` 标记并删除 outbox。
- `push --auto` 开始时若发现今天同 mode 的 outbox 且无 `.success`：**续传**——直接用 outbox 里存的块（保证 `（i/N）` 前缀一致），从第一个未发块继续，不重新生成内容。
- 测试：中途失败留 outbox（含已发 message_id）；下次调用只发剩余块；全成后 outbox 清理 + marker 写入；outbox 与新数据冲突时以 outbox 为准（当天内容一致性优先）。

**S3 旧事件评分审计回填（只做幂等部分，不伪造）**：
- 新增 `radar migrate --backfill-audit`：对缺 `score_audit` 的事件，用已存字段（audience_side/scale_hint/format/event_type/city）**只重放幂等上限类修正**（supply cap、training cap、沙龙 cap 7、0–10 夹取），减法类修正（周边 -1、小型 -2、邀约 -1）**不重放**（非幂等，无 raw 分无法安全应用）。结果写 `score_audit: {backfilled: true, caps_applied: [...], final: {...}}`；分数只可能下降或不变，Tier 随之重算。变化写 `logs/migrate.jsonl`。幂等：二跑 changed=0。
- 测试：cap 生效降分、已合规不变、二跑幂等、减法修正绝不重放。

**S4 运行克隆依赖自更新**：
- `scripts/push_local.sh` 在 pull 成功后：`shasum -a 256 pyproject.toml` 与 `.venv/.pyproject.sha256` 比较，不一致（或 `.venv` 缺失）→ `python3 -m venv .venv`（缺失时）+ `./.venv/bin/python -m pip install -q -e .` + 更新 sha 文件；安装失败 → 日志记录并继续用旧环境（不中断推送）。`.venv/.pyproject.sha256` 无需 gitignore（.venv 已忽略）。
- 测试：zsh 脚本逻辑用 `zsh -n` + 一个可注入路径的最小执行测试（tmp 目录假 pyproject/假 venv 标记，验证 sha 判定与更新；pip 安装用 stub 脚本替身）。

完成后 `PROGRESS-v2.md` 追加 `## 遗留风险加固轮 Report`：S1–S4 逐条证据、全套单测结果、外层应提交文件清单与建议提交信息。

## 16. ChatGPT Ads 垂直重排轮（Stan 业务收窄指令，2026-08-24，第十次派发）

Stan 明确：业务垂直于 ChatGPT Ads 广告（汇话客/conversaic，两种模式：自投·开户托管 / 代投·全案托管；真实资质 = 2026.06 ChatGPT Ads Beta 投放资格）。所有活动按"对 ChatGPT Ads 获客/分销的价值"重排。改动：

**T1 评分语义收窄**（`config/scoring.yaml` + 打分 prompt）：
- `acquisition_score` = ChatGPT Ads 潜在买家密度：P0 出海广告主/品牌 marketing 负责人/投放操盘手 0.65；P1 独立站/亚马逊/Shopify 卖家（有投放预算的）0.35；P2 新能源线**删除**（权重 0，不再单列）。
- `ecosystem_score`：`platform_presence` 保持 0.55 但语义改为"OpenAI 官方 > 其他 AI 平台 > 广告平台（Google/Meta/TikTok）官方在场"；`channel_ecosystem` 0.45 = 可分销 ChatGPT Ads 的渠道（海外营销代理商、出海服务商、跨境物流/支付等有客户群的服务商）；`ai_developers` 删除。
- reason 生成指令改为：两句话必须回答"这场会对卖 ChatGPT Ads 有什么用"——第一句谁在场（买家/渠道），第二句具体动作建议（如"适合带报价单现场约深聊"/"适合认识可分销的代理商"）。
**T2 全量重打分**：对现有全部 active/expected 事件重打分（用 `score_history` 记录，旧分保留），Tier 重算。运行 `radar run --live` 拿最新候选一起进。
**T3 重排推送**：完成后生成一版特殊 full 推送（标题"📡 活动雷达｜ChatGPT Ads 垂直重排版"），按新分排序、A 级带新 reason；dry-run 落盘即可，**发送由 Claude 外层执行**。
禁区同 §2/§8。测试更新到新权重语义，全套保持绿。报告追加 `## 垂直重排轮 Report`（含重排前后 Tier 变化对照表）。

## 17. 代理失效兜底（2026-08-25，第十一次派发，小修）

实测：`~/.hermes/.env` 注入的代理（127.0.0.1:7890）进程端口未监听时，`hermes send` 报 `Cannot connect to host 127.0.0.1:7890`；同时实测**清空代理环境变量后直连发送成功**（DNS 已正常解析）。修复：`send_via_hermes` 的每块发送若失败且错误文本含 `Cannot connect to host 127.0.0.1`（代理不可达特征），立即用剥离了 `HTTP_PROXY/HTTPS_PROXY/ALL_PROXY/http_proxy/https_proxy/all_proxy/NO_PROXY` 的环境重试一次（`subprocess.run(..., env=...)`，且设 `NO_PROXY=*` 防 hermes 自读 .env 覆盖——若 hermes CLI 自己加载 .env 导致 env 剥离无效，则在日志写明 `proxy_bypass_ineffective` 并按原失败路径处理，不猜测）。该直连重试不计入频控重试梯子。配单测（fake subprocess 两次调用断言 env 差异）。全套保持绿。报告追加 `## 代理兜底 Report`。

## 18. 相邻价值降权保留轮（Stan 校准，2026-08-25，第十二次派发）

Stan 反馈 §16 重排砍得过狠："其他的会也可以加，只是权重可以降低"——非 ChatGPT Ads 直接相关的会不应清零消失，应降权保留。设计：

**U1 评分语义修正**（scoring.yaml + prompt）：`ecosystem_score` 增加第三成分 `adjacent_industry` 权重 0.15（platform_presence 0.50 / channel_ecosystem 0.35 / adjacent_industry 0.15）：AI 行业大会、平台方开发者活动、出海生态圈层——"没有直接买家但值得保持在场感"的会给 2-5 分基础分，不清零。prompt 明确：非广告主场合不打 0，按相邻价值评分；reason 第二句对这类会写"在场感/圈层价值"而非硬凑获客理由。
**U2 定向重打分**：只对 §16 后被降为 D 且旧分（score_history 里 chatgpt_ads_v1 之前的记录）曾为 A/B 的事件重打分（预计 ~15 条，比全量省 token）；目标是它们大多回到 C（或个别 B），不是回到 A。Tier 规则不变。
**U3 展示**：确认 site 时间轴显示 C 级（现状已显示）；D 仍隐藏；推送仍只 A/B——C 级会在网页可见即满足"可以加"。重生成 site 与 dry-run 样张，不外发。
测试更新，全套绿。报告追加 `## 相邻价值轮 Report`（含被拯救回 C/B 的清单）。禁区同 §2/§8。
